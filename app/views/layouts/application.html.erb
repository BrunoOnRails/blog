<!DOCTYPE html>
<html>
  <head>
    <title>Aprendendo Ruby on Rails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "turbo", type: "module" %>

  </head>

  <body>
    <div class="bg-primary text-white text-center mb-1 p-1">
      <%= link_to root_path, class: "text-white link-underline link-underline-opacity-0" do%>
        <h1 class="display-6" >Aprendendo Ruby on Rails</h1>
      <% end %> 
      <p class="lead">
        Aqui estará todo meu conhecimento com Ruby on Rails
      </p>

      <nav class="navbar container" id="busca">
        <div class="container-fluid busca">
          <div></div>
            <%= form_with url: search_posts_path, method: :get, local: true, class: 'form-inline d-flex' do |form| %>
              <%= form.search_field :q, class: 'form-control me-2', required: true, placeholder:'Buscar', value: params[:q] %>
              <%= form.submit 'Buscar', class: 'btn btn-success', data: {disable_with: 'Buscando...'} %>
            <% end %>
        </div>
      </nav>

    </div>

    <div class="container" >
      <% flash.each do |type,message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <%= yield %>
    </div>  
    
    <footer class="bg-primary text-white text-center mt-5 p-1">
      Copyright © Bruno César <%= Date.current.year%>.
    </footer>

  </body>
</html>
